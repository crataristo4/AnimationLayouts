<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hard.code.tech.animationlayouts">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CeramicsActivity"></activity>
        <activity android:name=".activities.ShopDetailsActivity" />
        <activity android:name=".PageFourActivity" />
        <activity
            android:name=".PageThreeActivity"
            android:label="@string/title_activity_page_three" />
        <activity android:name=".PageTwoActivity" />
        <activity android:name=".StartScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PageOneActivity" />
    </application>

</manifest>
